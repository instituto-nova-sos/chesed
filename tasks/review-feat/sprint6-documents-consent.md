# Critical Review — feat/sprint6-documents-consent

_Generated by the autonomous-critical-review skill over `git diff main...HEAD`._
_Branch: feat/sprint6-documents-consent · Reviewed commits: main..HEAD · Date: 2026-07-04_

_Cycle 1 (2026-07-04) verdict was REQUEST_CHANGES: **MAJOR-1** hardcoded S3 credential
defaults in `config.go` (CLAUDE.md MUST NOT #2, docs/19 §Secret Management Rule 1) and a
required fix closing the Integration Test Mandate campus-boundary gap on the three list
GETs. Remediation landed as `5035701` (RED) → `8a1621e` (GREEN) and is verified below.
This cycle-2 report supersedes cycle 1; the verdict at the end is authoritative._

## Quality Gate: PASS

### Conditions
| Condition | Status | Details |
|-----------|--------|---------|
| Bugs | PASS | 0 — repository column/Scan/RETURNING parity verified by inspection (consentColumns 14/14, documentColumns 15/15, INSERT param counts correct) and by the real-Postgres integration suite (Sprint 5 B1 regression class covered). Production router topology (sibling `/persons/{id}/consents|documents` registered next to the mounted `/persons` group) empirically verified with chi v5.3.0: all 16 route shapes dispatch correctly, no shadowing of pre-existing person routes, no registration panic — this mattered because the integration harness builds a flat router and never exercises the production mount layout |
| Vulnerabilities | PASS | 0 — cycle-1 MAJOR-1 resolved: `config.go` now reads `S3_ACCESS_KEY`/`S3_SECRET_KEY` exclusively via `os.Getenv` with **no source-code defaults**; `validate:"required"` fails boot when absent (fail-fast restored, matching the DATABASE_URL/KEYCLOAK_* pattern). Grep confirms no credential literals remain in `config.go`; non-secret endpoint/bucket defaults retained; compose stacks inject the values at runtime |
| Security Hotspots | PASS | 7/7 reviewed: (1) magic-byte sniff — `http.DetectContentType` over first 512 bytes, seek-to-start rewind, client Content-Type ignored, spoofed `.pdf` text rejected at unit + integration layers, empty file rejected via `io.ReadFull` EOF; (2) `http.MaxBytesReader` installed before `ParseMultipartForm` (10MB file + 1MB multipart headroom), per-file `header.Size` check separate; (3) no storage-key leak — `FilePath`/`UploadedBy` are `json:"-"`, asserted by unit and integration tests; (4) presigned TTL 15 min pinned in unit test and `X-Amz-Expires` asserted against real MinIO; object keys are UUID-based `documents/{campus}/{person}/{uuid}{ext}` from sniffed type only; (5) no PII in logs/errors — messages generic (T3/T13), orphan-object log carries key/UUIDs only; (6) `signature_data` never SW-cached — `vite.config.ts` untouched, runtimeCaching regex still `^/api/v1/(service-types|campuses|campaigns)`, consent fetch disabled offline, signature never rendered or logged client-side; (7) S3 credential sourcing — remediated and re-reviewed: env-only, boot-time required validation, docs/19-compliant |
| Coverage | PASS | New-code coverage ≥80% across layers: consent service 75–100% per function (unit), document service 78–100% (unit; `ListByAttendance` covered at integration layer), handlers exercised by unit + real-stack tests; repositories covered by the integration suite per the established pattern. Integration Test Mandate now fully met: every new endpoint has happy path, campus boundary, documented error codes, and constraint coverage — the cycle-1 list-GET boundary gap is closed by `TestConsentList_CrossCampusNotFound` and `TestDocumentList_CrossCampusNotFound` (person + attendance), both passing against real Postgres+MinIO (4.7s targeted run). Suites: backend unit green (incl. rewritten `TestLoadS3` with fail-fast cases), backend integration green (full run 70.0s in cycle 1; remediation-scope re-run green), frontend 184/184 unit + 36/36 integration (untouched by remediation) |
| Duplication | PASS | ≤3% — consent/document follow the established handler/service/repository patterns; remediation reused the existing harness helpers (`seedSecondCampus`, claims overrides) and extracted `clearBaseConfigEnv` instead of duplicating env baselines; only smell is the duplicated 6-line private `audit()` helper across the two services (SUGGESTION) |
| Maintainability | PASS | A — all files within thresholds; `make lint` clean on remediated HEAD; no new ESLint warnings (49 = pre-existing baseline, none in branch files); the dead `validate:"required"` tags flagged in cycle 1 are now live validation |
| Reliability | PASS | A — every error handled (no `_` discards), consistent sentinel-error mapping to the documented HTTP contract, race-safe revoke (`is_active = TRUE` predicate re-checked in UPDATE), storage-first persistence with orphan-key logging, stale-response guards in both new hooks, boot now fails fast on missing storage credentials instead of proceeding on dev defaults |
| Security | PASS | A — RBAC role sets match docs/16 exactly (person doc upload Secretary+, attendance upload + all reads Professional+, consent create All / list Secretary+ / revoke ADMIN); campus scoping on every new query, now integration-proven on all read and write paths including the three list GETs; ACCESS_DENIED audited (integration-asserted); audit CREATE/UPDATE on all mutations with old/new values; no secrets in source |

### Clean Code Assessment
| Category | Status | Findings |
|----------|--------|----------|
| Consistency | PASS | Handlers mirror sibling patterns (`validateStruct` → service → `writeXError` sentinel switch); services follow the `CampaignService` shape (interface-at-consumer, `AuditParams`, campus from claims); repositories follow the columns-constant + `scanX` lockstep convention; hooks reuse the `useCampaign` stale-guard pattern; MSW tests follow shared `server.ts` + per-test `.use(...)`. The cycle-1 deviation (credential defaults vs the required-no-default pattern) is resolved — S3 credentials now follow the same pattern as every other credential-bearing setting |
| Intentionality | PASS | Names reveal purpose (`resolvePersonRef`, `storeAndPersist`, `uq_consent_active_person_type`, `clearBaseConfigEnv`); comments encode non-obvious decisions only (chi sibling-mount rationale, storage-first ordering, race-safe revoke predicate, jsdom/undici multipart realm note, docs/19 credential-sourcing rationale); no dead code — the previously-dead `required` tags now enforce |
| Adaptability | PASS | `ObjectStorage` interface defined at the consumption site (service pkg), MinIO implementation isolated in `internal/storage` — swapping to S3/R2 is a wiring change; frontend API isolated in `api/documents.ts`/`api/consents.ts`; hooks absorb contract changes; label/preset maps centralized in `utils/` |
| Responsibility | PASS | No layer violations: handlers parse/sniff/map only, services own rules and audit, repositories own SQL; magic-byte sniff correctly placed at the HTTP boundary with a service-side whitelist re-check as defense in depth; frontend sections delegate fetch to hooks, dialogs own only their local state (`useRevokeAction` extracted to keep the section within budget) |

### Issues

**BLOCKER** — none.

**MAJOR** — none. Cycle-1 MAJOR-1 (hardcoded S3 credentials, `backend/internal/config/config.go:40-41`) is **RESOLVED and verified in code**, not from commit messages:
- Grep of remediated `config.go`: zero credential literals; `S3AccessKey`/`S3SecretKey` are `os.Getenv` with `validate:"required"` intact (`backend/internal/config/config.go:41-42`).
- RED proof reproduced in a throwaway worktree at `5035701`: `TestLoadS3/missing_credentials_fail_fast` and `TestLoadS3/missing_secret_key_fails_fast` fail there for the expected reason ("An error is expected but got nil" — defaults satisfied `required` pre-fix) and pass on HEAD.
- The GREEN commit's only test-file change is a pure refactor (inline env baseline → `clearBaseConfigEnv` helper); no assertion was weakened.
- Cycle-1 required fix 2 (list-GET campus boundary) is **RESOLVED**: `backend/internal/integration/consent_test.go` `TestConsentList_CrossCampusNotFound` (404 via foreign-campus claims) and `backend/internal/integration/document_test.go` `TestDocumentList_CrossCampusNotFound` (person and attendance list GETs → 404) — both pass against the real harness and pin the campus-scoped resolution in the service list methods.

**MINOR** (open, documented follow-ups — none blocking)

1. `backend/internal/repository/consent_repository.go:26-28` (via `handler/consent.go:61`) — `GET /persons/:id/consents` returns `signature_data` (CRITICAL PII, base64 PNG) for every registry row. The UI never renders it, and the docs/11 list example omits it. Recommend excluding it from the list envelope (keep it on the 201 echo) or explicitly documenting its inclusion; also a payload-weight concern for long registries.
2. `backend/internal/service/consent_service.go:59-61,173-175` — struct-validation failures are returned as wrapped raw validator errors, not `domain.ErrValidation`; unreachable via HTTP today (handler validates first) but a direct service consumer would surface 500 instead of 400. `documentService.validateUpload` wraps correctly — align for consistency.
3. `frontend/src/types/document.ts:16-17` — `file_size: number` and `mime_type: string` typed non-nullable while the DDL and Go domain (`*int64`, `*string`) allow NULL. Harmless on today's upload path (always set) but a latent runtime/type mismatch.
4. Commit `9ddb1cb` (mount: `App.tsx`, `PersonDetailPage.tsx`, `types/index.ts`) — production files with no dedicated RED and no `No-Test-Rationale:` trailer. Content fits the tdd-enforcement.md "scaffolding/route wiring" exception, but the rule mandates the explicit trailer for auditability. Historical commit; recording here rather than rewriting history, per the rule's own guidance.
5. `docs/16-iam-and-access-control.md` consent matrix — "View consents — Professional: Own assigned" vs implementation (campus-wide via the SECRETARY+ role set, consistent with every other surface). Pre-existing matrix wording; align docs or scope in a future slice.

**SUGGESTION**

1. `frontend/src/components/consents/SignaturePadCanvas.tsx:94-111` — a tap without movement leaves no ink and emits nothing (deliberate, pinned by test) and the drawing buffer is sized once on mount: after an orientation change/resize, strokes are offset until remount. Consider a ResizeObserver re-setup.
2. `backend/internal/service/consent_service.go:210-216` / `document_service.go:242-248` — identical private `audit()` helper duplicated; could live on `AuditService` (e.g. `LogActionBestEffort`).
3. `docker-compose.yml:60-62` MinIO dev credentials follow the pre-existing dev-compose convention (`POSTGRES_PASSWORD: chesed`, `KEYCLOAK_ADMIN_PASSWORD: admin` already on main) — acceptable for the dev stack, but worth a tech-lead pass against docs/19 Rule 1 scope at sprint boundary.

### Complexity Report

No function exceeds the enforced thresholds.

- **Go**: `make lint` clean on remediated HEAD (cyclop ≤10, funlen ≤60 as configured in `.golangci.yml` with in-file rationale — the docs/quality table still says 40; pre-existing, documented drift, not introduced by this branch; nestif ≤4). Largest new functions: `storeAndPersist` ~44 code lines, `parseUploadForm` ~30, `buildConsentFromInput` ~25 — all within limits, nesting ≤2, params ≤5. Remediation added no production functions beyond the two-line `Load` field changes; `clearBaseConfigEnv` is a 12-line test helper.
- **React/TS**: no new ESLint warnings (49 total = pre-existing baseline; zero in branch files). Files: `ConsentFormPage.tsx` 207 lines, `DocumentUploadModal.tsx` 161, `SignaturePadCanvas.tsx` 163 — all <300. `ConsentFormPage` component fn ~66 code lines — within the configured `max-lines-per-function: 80` (docs table says 50; the 80 config predates this branch). All JSX return blocks ≤80 lines; sub-components (`ConsentFields`, `FileField`, `DocumentsPanel`, `useRevokeAction`) extracted to stay within budget.

### TDD Narrative

RED→GREEN ordering holds for every story and for the remediation cycle, verified from `git log --name-only` (RED commits contain test files only; GREEN commits production files only or, in the remediation GREEN, production + a non-weakening test refactor):

- **S08.1**: `3ce6c12` test (RED: config_test, storage integration) → `bf87250` feat (GREEN: config, ObjectStorage interface, MinIO impl, compose)
- **S08.3/S08.4**: `27f6de2` test (RED: handler/service/integration consent tests + harness wiring) → `781c3e3` feat (GREEN: consent full stack + migration 000024)
- **S08.5**: `192099f` test (RED) → `d76de03` feat (GREEN: documents UI)
- **S08.6/S08.7**: `135e376` test (RED) → `d270201` feat (GREEN: consent UI + signature pad)
- **S08.2**: `ac94830` test (RED) → `67e1414` feat (GREEN: document API + migration 000025) — lands after the frontend pairs; story-scoped ordering holds
- **Remediation (review MAJOR-1 + mandate gap)**: `5035701` test (RED: fail-fast credential cases + three list-boundary integration tests) → `8a1621e` fix (GREEN: env-only credentials). RED failure **empirically reproduced** in a throwaway worktree at `5035701`: the two `wantErr` subtests fail pre-fix for the expected reason and pass on HEAD; the boundary integration tests pass against real Postgres+MinIO.

Gap (unchanged, MINOR issue #4): `9ddb1cb` mount commit ships production files with no RED and no `No-Test-Rationale:` trailer — composition/wiring of already-tested components, exception applies in substance but not in form.

Validation evidence (all run locally, CI paused): `go build` ✓ · `go test ./internal/config/` ✓ (rewritten `TestLoadS3` green) · `go test -short ./...` ✓ (0 failures) · `make lint` ✓ (0 findings) · full `go test -tags integration` against real Postgres + MinIO testcontainers ✓ (70.0s, cycle 1) · targeted remediation re-run `-run 'TestDocumentList_CrossCampus|TestConsentList_CrossCampus'` ✓ (4.7s, 2/2 incl. both list surfaces) · RED-commit worktree check ✓ (fails pre-fix as required) · chi v5.3.0 route-topology scratch harness ✓ (16/16 dispatch) · `npx tsc --noEmit` ✓ · `npm test` 184/184 ✓ · `npm run test:integration` 36/36 ✓ · `npm run lint` 0 errors / 49 warnings (baseline) ✓ · `vite.config.ts` unchanged (SW never caches consent/document responses) ✓ · frontend untouched by the remediation commits, cycle-1 evidence stands. E2E smoke was not run in this review; it remains a blocking, Docker-gated step of `make deliver` (rebuild the e2e stack first).

### Verdict: APPROVE

Both cycle-1 required fixes are resolved and verified in code with genuine RED→GREEN evidence. No BLOCKER or MAJOR issues remain. The five MINOR items and three SUGGESTIONs above are non-blocking follow-ups — fold them into the sprint-boundary quality pass or the next slice touching these surfaces.
