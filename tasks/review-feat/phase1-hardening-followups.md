# Critical Review — feat/phase1-hardening-followups

_Generated by the autonomous-critical-review skill over `git diff main...HEAD`._
_Branch: feat/phase1-hardening-followups · Reviewed commits: main..HEAD · Date: 2026-07-01_

## Quality Gate: PASS

### Conditions
| Condition | Status | Details |
|-----------|--------|---------|
| Bugs | PASS | No defects found. All denial paths audited; all reference paths campus-checked; nil-safe throughout. |
| Vulnerabilities | PASS | Closes Finding 3 (A01 cross-campus IDOR via sync) and Finding 4 (A09 missing audit trail). No new vulns. |
| Security Hotspots | PASS (reviewed) | Migration additive-only (append-only audit_log preserved); `extractIP` uses non-spoofable `RemoteAddr`; error wording does not leak cross-campus existence. |
| Coverage | PASS | 3 unit tests + 1 integration test for Track 1; table-driven unit test (4 cases) + 2 integration tests for Track 2. New branches all exercised. |
| Duplication | PASS | `requirePersonInCampus`/`requireTriageInCampus` share the same intentional idiom; ~3 lines each, not extract-worthy. |
| Maintainability | PASS | `buildSyncAttendance` refactor *lowered* complexity by extracting `resolveAttendanceRefs`. All functions within thresholds. |
| Reliability | PASS | Every error handled; swallow-and-log audit idiom matches existing `logCreate` pattern; no `_`-discarded errors. |
| Security | PASS | Rating A. Campus scoping enforced at service layer with DB-level FK/scoped-lookup defense in depth. |

### Clean Code Assessment
| Category | Status | Findings |
|----------|--------|----------|
| Consistency | PASS | `FindByID` interface signatures exactly match the pre-existing concrete `PersonRepository.FindByID`/`TriageRepository.FindByID` (campus-scoped, `ErrNotFound` on no rows). Audit swallow-and-log mirrors `syncService.logCreate`. IP-in-audit matches the `ip_address INET` column present since migration 000010. |
| Intentionality | PASS | Names reveal purpose (`requirePersonInCampus`, `resolveAttendanceRefs`, `auditDenial`). Comments cite the specific finding/threat (Finding 3/T3, Finding 4) and encode the non-obvious "generic error, never reveal foreign existence" decision. No dead code. |
| Adaptability | PASS | Dependency direction correct: handler→service→repo interface; middleware→`*service.AuditService`. `RequireRole` takes the audit dep explicitly (no global/singleton — respects CLAUDE.md rule #1). |
| Responsibility | PASS | `resolveAttendanceRefs` isolates parse+campus-validate; `buildSyncAttendance` now only maps; `auditDenial` does one thing. |

### Issues

**BLOCKER:** none.
**MAJOR:** none.

**MINOR**
- `sync_service.go` — `requirePersonInCampus` and `requireTriageInCampus` are structurally identical except the repo call and message string. Acceptable as-is (2 repos, 2 distinct messages, ~9 lines each), but if a third reference type appears, fold into one helper parameterized by a `func(ctx, id, campusID) error` + label. Not blocking.

**SUGGESTION**
- `main.go` — `RequireRole(d.auditSvc, ...)` now threads the audit dep through the RBAC call sites. A future refactor could bind `auditSvc` once via a small constructor closure (`rbac := middleware.NewRBAC(auditSvc); rbac.Require("ADMIN")`) to avoid repeating the dep at every call site. Cosmetic only.
- `rbac_audit_test.go` / `sync_test.go` (integration) — solid, but consider also asserting the persisted `ip_address`/`entity_type='rbac'` on the denial row to lock the full audit contract. The unit test already asserts `ActionType=="ACCESS_DENIED" && !Success`, so coverage of the security property is not gapped.

### Complexity Report
All functions within thresholds (Go: cyclomatic ≤10, cognitive ≤25, function ≤40 lines, file ≤400):
- `buildSyncTriage` — cyclomatic ~7; one guard call added, within cognitive limit.
- `buildSyncAttendance` — ~35 lines, cyclomatic ~4. **Reduced** by the `resolveAttendanceRefs` extraction.
- `resolveAttendanceRefs` — ~35 lines, cyclomatic ~7 (new function, under 10).
- `requirePersonInCampus` / `requireTriageInCampus` — ~9 lines, cyclomatic 3 each.
- `RequireRole` — ~20 lines, cyclomatic 2.
- `auditDenial` — ~18 lines, cyclomatic 3 (nil-guard + write-error branch).
- `sync_service.go` file length exceeds the 400-line guideline, **but this is a pre-existing condition on `main`, not introduced or materially worsened by this branch** (net change is a small refactor + two ~9-line guards). Flagged for awareness; splitting the file is out of scope for a security-hardening follow-up.

### TDD Narrative
RED→GREEN order holds per track, confirmed against commit history:

- **Track 1 (Finding 3)** — `test: ...(RED)` touches test files only (`sync_service_test.go`, `sync_test.go`); `sync_service.go` is NOT in the RED commit. The RED tests fail at runtime because pre-fix code never calls `FindByID` and persists the record (unmet mock expectation + wrong status). GREEN adds the `FindByID` interface methods, the two guard helpers, and back-fills happy-path `FindByID` stubs.
- **Track 2 (Finding 4)** — `test: ...(RED)` adds `wantAudited` cases + integration tests (watched RED against a temporary no-audit stub: the three denial cases fail because the audit write is never made); `fix: ...(GREEN)` threads `auditSvc`, adds `auditDenial`, and migration 000020.
- **Track 3 (docs)** — the `docs:` commit is last. Correct.

### Adversarial checks performed and cleared
- Both `triage.person_id`, `attendance.person_id`, and `attendance.triage_id` guarded — no bypass path to persistence.
- No cross-campus existence leak — identical generic rejection wording whether the row is absent or in another campus.
- Audit fires on ALL denial branches (lacks-role and no-claims-in-context); `ClaimsFromContext` returns a value (never nil), `user_id`/`campus_id` are nullable so the write succeeds with null actor. No panic.
- `auditDenial` early-returns on nil `auditSvc`; production wires a real one.
- Append-only rule (#6) intact — migration only swaps a column CHECK constraint; constraint name `audit_log_action_type_check` matches `000010`; down migration restores the original 8-value set.
- No PII (rule #7) in the audit entry or log lines.

### Verdict: APPROVE
The two hardening follow-ups are correctly and completely implemented under genuine RED→GREEN TDD. Finding 3 is closed for all three reference paths with no cross-campus leak; Finding 4 records `ACCESS_DENIED`/`success=false` on every denial branch with a nil-safe, non-request-failing audit write. The migration is additive and preserves the append-only invariant. Tests assert the actual security properties (no row persisted on rejection; denial row written; allowed request writes none) — no false-GREEN risk. No BLOCKER or MAJOR issues. The MINOR/SUGGESTION items are non-blocking and out of scope for this hardening change.
