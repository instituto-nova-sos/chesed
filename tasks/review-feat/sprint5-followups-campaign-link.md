# Critical Review — feat/sprint5-followups-campaign-link

_Generated by the autonomous-critical-review skill over `git diff main...HEAD`._
_Branch: feat/sprint5-followups-campaign-link · Reviewed commits: main..HEAD · Date: 2026-07-03_

## Quality Gate: PASS

### Conditions
| Condition | Status | Details |
|-----------|--------|---------|
| Bugs | PASS | 0. Sprint-5 B1 regression class explicitly audited: `TriageRepository.CreateWithSync` (12 cols/12 args, RETURNING 2 → Scan 2), `AttendanceRepository.CreateWithSync` (13/13, 2→2), both `FindBySyncID` selects (13→13, 14→14) — all column/Scan aligned, `campaign_id` present in INSERT, SELECT, and Scan on both paths. Per-record atomicity proven at DB level (`COUNT(*)=0` after rejection). |
| Vulnerabilities | PASS | 0. Campaign reference resolved via campus-scoped `CampaignRefRepository.FindByID(ctx, id, campusID)`; foreign-campus and nonexistent IDs both surface the same generic `campaign reference not resolvable: validation error` (threat T3 — no cross-campus existence disclosure). No PII in error results or logs. E2E fixture SQL is parameterized. |
| Security Hotspots | PASS | 3/3 reviewed: (1) sync push external input — `campaign_id` validated `omitempty,uuid` then campus-scoped lookup before any write; (2) cross-campus guard reuses the hardened `resolveCampaignRef` (single validation path, no drift from the online create path); (3) e2e cleanup SQL — parameterized, prefix-scoped, FK-safe order (attendance/triage → campaign_team → campaign → person → app_user). |
| Coverage | PASS | New code covered at every mandated layer: 5 backend unit tests (persist link, cross-campus reject ×2, invalid UUID, attendance persist), 3 backend integration tests (executed green against real testcontainers Postgres in this review), 4 MSW integration tests, 4 offline sync-queue unit tests, 9 page component tests, 1 E2E smoke slice. Estimated new-code coverage well above 80%. |
| Integration tests | PASS | Backend: `sync_test.go` extends the real HTTP→service→Postgres suite for the new sync-push contract (happy path triage + attendance with DB-level `campaign_id` assertion, cross-campus rejection with zero-row assertion) — executed and PASS (13 campaign-related integration tests, 25.1s). Frontend: `campaignLink.integration.test.ts` pins the wire contract at the MSW network boundary (body carries `campaign_id` when linked, key **absent** when not — matching the backend's `omitempty,uuid` semantics). |
| Duplication | PASS | Production duplication ≈ 0% (campaign resolution centralized in `resolveCampaignLink` → `resolveCampaignRef`, one shared helper for triage and attendance). Test-scaffolding similarity between the two page test files noted as SUGGESTION below. |
| Maintainability | A | No new function exceeds any lint-enforced threshold (`golangci-lint` clean; `eslint` 0 errors). Pre-existing guideline tensions worsened marginally — see MINOR issues. |
| Reliability | A | Every error handled; per-record error semantics preserved (one bad record never fails the batch); offline selector fetch failure degrades to an empty list without blocking the create form; audit logging on all sync creates unchanged and exercised by unit tests. |
| Security | A | 0 security issues. Verified suites: backend `go test -short ./...` PASS, `make lint` clean, frontend 153 unit + 25 integration tests PASS, `tsc --noEmit` clean, backend integration campaign/sync tests PASS against real Postgres. |

### Clean Code Assessment
| Category | Status | Findings |
|----------|--------|----------|
| Consistency | PASS | Backend follows sibling patterns exactly: constructor injection (`NewSyncService` extended, wired identically in `main.go` and the integration harness), `errorResult`/`*domain.SyncPushResult` early-return convention, `omitempty,uuid` validation tags, campus-scoped repo interface at the consumption site. Frontend `Select` spread-registration matches the sibling service-type select on the same pages. One deviation: `useLinkableCampaigns` returns a bare array instead of the `{ data, isLoading, error }` hook shape — deliberate and documented, noted MINOR. |
| Intentionality | PASS | Names reveal purpose (`resolveCampaignLink`, `LINKABLE_CAMPAIGN_STATUSES`, `useLinkableCampaigns`, `syncAttendanceRefs.campaignID`). Comments explain *why*, not what: T3 rationale on `resolveCampaignLink`, the conditional-assignment-vs-`|| undefined` rationale on both pages (key absence is the wire contract), and the `UpdateTriageInput` exclusion comment — verified accurate against backend `UpdateTriageInput` (no `campaign_id` field). No dead code. |
| Adaptability | PASS | The sync service consumes the existing `CampaignRefRepository` interface rather than a concrete repo — the campaign lookup has exactly one implementation of the T3 rejection rule shared by triage online, attendance online, and both sync paths. Frontend change confined to the right layers: types → hook → page; offline modules untouched (payload rides through the existing `{...input}` pass-through). |
| Responsibility | PASS | No layer violations. `resolveCampaignLink` does one thing (validate + map error shape). Pages delegate campaign loading to the hook and submission to the offline-fallback module. `buildSyncTriage`/`resolveAttendanceRefs` remain single-purpose mapping functions. |

### Issues

**BLOCKER** — none.

**MAJOR** — none.

**MINOR**

1. `backend/internal/service/sync_service.go` (643 lines) — exceeds the documented 400-line file guideline (`docs/quality/complexity-guidelines.md`). Pre-existing on main (613 lines); this diff adds +30. Recommend a refactor pass splitting by entity within the package (`sync_triage.go`, `sync_attendance.go`, `sync_person.go`).
2. `backend/internal/service/sync_service.go:252` — `buildSyncTriage` is 55 raw lines vs the documented 40-line guideline. Passes lint because `.golangci.yml` sets `funlen: lines: 60` (comments/blanks ignored) with an in-config rationale; the guideline doc and the lint config disagree — pre-existing tension, +4 lines here. Align the doc or the config via the documented ADR path.
3. `frontend/src/pages/TriageCreatePage.tsx:23` (169 lines, max 80) and `frontend/src/pages/AttendanceCreatePage.tsx:22` (152 lines, max 80) — ESLint `max-lines-per-function` warnings. Both components already exceeded the limit on main (≈152/≈137); this diff grows them by ~15 lines each. Extracting a shared `CampaignSelect` (campaigns + registration props) would reverse the growth and remove the duplication between the two pages.
4. `frontend/src/hooks/useCampaigns.ts:67` — `useLinkableCampaigns` returns `CampaignListItem[]` instead of the `{ data, isLoading, error }` shape sibling hooks use. The deviation is documented (optional context, silent degradation) but weakens hook-shape consistency; consider returning `{ campaigns }` at minimum.
5. `frontend/src/hooks/useCampaigns.ts:72` + `frontend/vite.config.ts:47` — offline population of the selector depends on the service worker NetworkFirst runtime cache, which is keyed by the exact URL `/api/v1/campaigns?per_page=100`. That entry is only warmed after a create form is first opened online; the campaigns *list* page warms a different URL (`per_page=20`). Degradation is graceful (empty selector, form still submits without a campaign — covered by tests), but the S07.6 offline criterion implicitly assumes a prior online visit to a create form. Consider warming this exact URL in the pre-cache pass documented in `docs/12-offline-sync-strategy.md`.

**SUGGESTION**

6. `frontend/src/pages/__tests__/TriageCreatePage.test.tsx` / `AttendanceCreatePage.test.tsx` — the `campaign()`, `campaignsResponse()`, `renderPage()` scaffolding and mock wiring are near-duplicates; a shared fixture module under `__tests__/` would cut ~40 duplicated lines.
7. `frontend/src/__integration__/campaignLink.integration.test.ts:107-122` — `captureTriagePost`/`captureAttendancePost` differ only in path and echo body; a single `capturePost(path, extra)` helper would suffice.

### Complexity Report

No function in the diff exceeds the lint-enforced thresholds (`golangci-lint` gocognit 25 / cyclop 10 / funlen 60 / nestif 4 — clean run; ESLint complexity 10 / sonarjs 15 — 0 errors on changed files).

Measured on changed functions:
- `SyncService.resolveCampaignLink` — 8 lines, cognitive ≈ 1. OK.
- `SyncService.buildSyncTriage` — 55 raw lines (guideline tension, MINOR #2), cognitive ≈ 9, cyclomatic ≈ 10, nesting 1. Within enforced limits.
- `SyncService.resolveAttendanceRefs` — 42 raw lines, cognitive ≈ 8, nesting 2. OK.
- `useLinkableCampaigns` — 19 lines, cognitive ≈ 3. OK.
- Page `onSubmit` functions gain +1 branch each (`if (values.campaign_id)`), remaining well under 10.
- Documented-guideline overages are pre-existing and tracked in MINOR #1–#3; none exceed 2× any threshold.

### TDD Narrative

Commit order (chronological) tells a coherent RED→GREEN story, per-layer as required by `.project-ai/rules/tdd-enforcement.md`:

1. `e28744a` docs — backlog GWT + API/offline-sync payload docs (documentation-first, allowed exception).
2. `bcfe3e6` `test: S07.6 sync push campaign_id (RED)` — **test files only** (`sync_service_test.go`, `integration/sync_test.go`, `integration/harness_test.go`). Red for the expected reason: tests call the new 5-arg `NewSyncService` signature and assert `campaign_id` persistence that did not exist — compile failure of the test packages plus failing assertions (missing symbol = expected RED per rule).
3. `edc58c1` `feat: S07.6 accept optional campaign_id on sync push (GREEN)` — production only (`sync_service.go`, `cmd/server/main.go`).
4. `3373b7c` `test: S07.6 campaign E2E smoke slice` — test-only commit; exercises the pre-existing S07.3/S07.5 production surface. Selectors verified against production code: `Nova campanha` link (`CampaignListPage.tsx:96`), `Nome`/`Data de início` labels with explicit `id`/`htmlFor` (`CampaignFormPage.tsx:80-94`), `Salvar` button (`:190`), detail `<h1>` (`CampaignDetailPage.tsx:203`) — stable, role/label-based.
5. `a52acc2` `test: S07.6 campaign selector + offline campaign_id payload (RED)` — **test files only**. Page tests genuinely red (no selector rendered). Caveat noted: the offline-payload and MSW tests were red at the typecheck level only (`campaign_id` absent from `CreateTriageInput`/`CreateAttendanceInput` → `tsc` failure, i.e. missing symbol), while runtime would already pass due to the `{...input}` pass-through in `saveTriageOffline`/`saveAttendanceOffline`. Acceptable under the rule's "assertion failure or missing symbol" clause; flagged for the narrative's honesty.
6. `b48bfde` `feat: S07.6 campaign selector in triage/attendance forms (GREEN)` — production only.
7. `07f572c` docs — status flip to done.

First test-touching commit precedes first production commit for both layers; every RED commit contains only test files. Compliant.

### Contract Fidelity (S07.6 GWT → evidence)

- **Selector renders, optional, walk-in unchanged** → page tests (`renders the optional selector with only linkable campaigns`, `omits the campaign_id key`, `still submits when there are no campaigns`, fetch-failure resilience). Covered.
- **Online create carries campaign_id; detail returns it** → production path pre-existing from S07.3 (`triage_service.go:72`, `attendance_service.go:76`, `json:"campaign_id,omitempty"` domain tags); existing integration tests `TestTriageCampaignLink_OwnCampus` / `TestAttendanceCampaignLink` re-executed green; new MSW tests pin the request body; `Triage`/`Attendance` TS types now expose `campaign_id`. Covered.
- **Offline queue carries campaign_id; drain persists the link** → offline unit tests (queue `data` payload includes/omits the key) + `syncEngine` sends `data` verbatim (`syncEngine.ts:58`) + backend integration tests persist from the same payload shape. Covered by composition at each boundary.
- **Nonexistent/foreign campaign → per-record error, nothing persisted** → unit tests (ErrNotFound covers both cases — the campus-scoped lookup is intentionally indistinguishable, T3) + `TestSyncPush_CrossCampusCampaignReferenceRejected` with DB zero-row assertion. Covered.
- **E2E smoke slice: create → list → Postgres row** → `campaign-smoke.spec.ts` with DB-level campus + status assertion; fixture cleanup extended in FK-safe order. Covered.
- `docs/11-api-design.md` sync push section updated in the same branch and matches the implementation exactly (optional uuid in `data`, per-record `error`, nothing persisted). `UpdateTriageInput` exclusion on the frontend matches the backend input struct.

### Verdict: APPROVE

All quality gate conditions pass; no BLOCKER or MAJOR issues. The five MINOR findings are non-blocking follow-ups (file/function growth in `sync_service.go` and the two create pages, hook-shape consistency, and offline cache warm-up for the selector URL) and are recommended for the next refactor pass or backlog note.
