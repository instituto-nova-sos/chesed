# Critical Review — feat/autonomous-delivery-and-offline-sync-drainer

_Generated by the autonomous-critical-review skill over `git diff main...HEAD`._
_Branch: feat/autonomous-delivery-and-offline-sync-drainer · Reviewed commits: main..HEAD · Date: 2026-06-30_

Scope: full branch. Track A (S05.2 triage/attendance offline-create) was
reviewed and APPROVE'd earlier in the session; this regeneration covers the
Sprint 4 parallel-track increment delivered on top of it — **Track B (S05.5
status indicator), Track C (conflict-resolution UI), Track D (PWA + E2E)** — plus
the two review-nit fixes. Earlier branch commits (offline sync drainer, backend
lint-zero, autonomous delivery pipeline) had already passed their own reviews.

## Quality Gate: PASS

### Conditions
| Condition | Status | Details |
|-----------|--------|---------|
| Bugs | PASS | 0. No unhandled rejections; drainer excludes conflicted/dead-lettered items; `person?.full_name` narrowing correct. |
| Vulnerabilities | PASS | 0. No secrets, no PII in logs, no injection sinks. `@resvg/resvg-js` is a devDependency (build-time only). |
| Security Hotspots | PASS | 100% reviewed. Only new external-input surface is the workbox `urlPattern` regex — verified anchored and scoped to `/service-types` + `/campuses`. E2E fixtures are test-only. |
| Coverage | PASS | Every new module has co-located tests: SyncStatusBanner (8), useSyncConflicts (3), SyncConflictsPage (4), requeueConflict (dedicated), InstallPrompt (3), offline modules 100% lines. `src/offline/**` held at the enforced 80% floor. |
| Duplication | PASS | ≤3%. Track B extracted badge/indicator/title/guard helpers — a net reduction. |
| Maintainability | PASS (A) | No new function exceeds thresholds (see Complexity). |
| Reliability | PASS (A) | Every async path handled; cache-on-success is intentional fire-and-forget. |
| Security | PASS (A) | No `any`, no PII leakage, no unguarded endpoints. |

### Clean Code Assessment
| Category | Status | Findings |
|----------|--------|----------|
| Consistency | PASS | `useSyncConflicts` follows the sibling fetch-hook shape (its single set-state-in-effect warning is identical to the documented baseline); `ConflictList`/`ConflictRow` mirror existing list decomposition. |
| Intentionality | PASS | `bannerIsHidden`, `requeueConflict`, `syncNowTitleFor` reveal purpose; comments explain only the non-obvious decisions (mid-drain visibility; SW cache ownership boundary). No dead code. |
| Adaptability | PASS | Dependencies point inward: page → hook → syncEngine → db. Conflict badge is a `Link`, decoupling banner from page. |
| Responsibility | PASS | Each function does one thing. |

### Issues
**BLOCKER:** none. **MAJOR:** none.

**MINOR (resolved in-branch)**
1. docs/12 §"Service Worker Caching" table reconciled: network-first now stated
   for reference GETs (`/service-types`, `/campuses`) only; entity collections
   are served by the Dexie layer so the SW cache never masks unsynced records.

**INFO (resolved in-branch)**
2. `requeueConflict` now resets `retryCount: 0` on operator resubmit (TDD), so a
   previously error-bumped item does not start near the dead-letter ceiling.
3. `item.id as number` cast in `ConflictList.tsx` — safe (every conflict is a
   persisted Dexie row with an auto-increment id). Cosmetic; left as-is.

### Complexity Report
All new/changed functions within React/TS thresholds (cyclomatic ≤10, function
≤50 lines, JSX ≤80, nesting ≤3, params ≤5): `SyncStatusBanner` ~30 lines /
cyclo ~5; `requeueConflict` ~14 / cyclo 3; `useSyncConflicts` ~35 / cyclo 2;
`SyncConflictsPage` JSX ~24; `ConflictList`/`ConflictRow` cyclo 1; `InstallPrompt`
JSX ~22. No violations. Lint independently verified: 0 errors (50 warnings, below
the 51 baseline — Track B removed the banner complexity warning; the
eslint-ignore chore removed 3 generated-file warnings).

### TDD Narrative
RED→GREEN verified per slice by reconstructing each RED tree (production symbols
absent → compile-fail RED) then present in GREEN:
- **Track B:** RED `630c6b9` (test only) → GREEN `544bd3c` (production only).
- **Track C:** RED `4af1414` (three test files; `SyncConflictsPage`/`useSyncConflicts`/`requeueConflict` absent) → GREEN `3625fd5`.
- **Track D:** RED `fbe0bb6` (`InstallPrompt.test.tsx`; component absent) → GREEN `dca1599`; E2E offline-triage slice + SW-scoping fix in `f5f37e1`.
- **Nit fixes:** requeueConflict retryCount reset landed test-first (seeded
  `retryCount: 2`, asserted reset → RED → GREEN).
All satisfy `.project-ai/rules/tdd-enforcement.md`.

### Design decision — SW cache scoping (E2E-surfaced fix)
Correct and airtight. Caching all `/api/v1/*` GETs made the SW and the Dexie
cache both own person/triage/attendance reads; offline, the SW served a stale
list and masked the IndexedDB fallback that holds unsynced records. Scoping
NetworkFirst to `/service-types` + `/campuses` (read-only reference data, no
IndexedDB layer) removes the double-ownership; the anchored regex matches only
those prefixes. Verified by the person-offline E2E slice regressing on the broad
config and passing on the scoped one. docs/12 reconciled (MINOR-1 above).

### Verdict: APPROVE
Zero BLOCKER/MAJOR. All New Code gate conditions pass; all acceptance criteria
for S05.3/S05.4/S05.5 and roadmap tasks 4.4/4.7 met and exercised by unit +
real-stack E2E; RED→GREEN order holds for every slice; lint 0 errors. The two
review nits were fixed in-branch. Local gates green: 109 unit / 12 integration /
coverage ≥ floors / typecheck / 0 lint errors / build / backend build+lint+test /
**E2E smoke 3/3 against the real Docker stack** (person online, person offline,
triage offline).
